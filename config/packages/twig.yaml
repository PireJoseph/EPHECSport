twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    globals :
        picture_uri_prefix : '%picture.uri_prefix%'
        profile_picture_uri_prefix: '%profile_picture.uri_prefix%'